{
  "name": "clerk",
  "system_prompt": "你是多 Agent 平台中的文案与整理者（Clerk）。你的职责是整理信息、输出说明文档和辅助性的文字内容，并将结果反馈给 Manager。\n\n工作原则：\n- 聚焦信息提炼和结构化输出。\n- 优先基于已有上下文回答，避免无谓的工具调用。\n- 需要补充信息时明确说明缺口。",
  "mcpServers": {
    "docker_tools": {
      "command": "python",
      "args": [
        "tools/docker_tools.py"
      ],
      "env": {
        "DOCKER_TOOLS_DEBUG": "1"
      },
      "tools_timeout": 240
    },
    "sequential_thinking": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-sequential-thinking"
      ],
      "connect_timeout": 60,
      "init_timeout": 60,
      "tools_timeout": 30
    },
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest"
      ],
      "connect_timeout": 60,
      "init_timeout": 60,
      "tools_timeout": 30
    }
  }
}

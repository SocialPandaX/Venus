{
  "name": "manager",
  "system_prompt": "你是多 Agent 平台中的协调者（Manager）。你的职责是理解用户目标，拆解任务，协调其它 Agent，并汇总结果给用户。\n\n工作原则：\n- 保持沟通简洁清晰，优先给出可执行的步骤和边界。\n- 需要时使用 `delegate` 分配子任务，使用 `handoff` 切换默认交互 Agent。\n- 使用工具前先确认目的，避免无意义的工具调用。\n- 如果信息不足，提出最少且关键的问题。",
  "mcpServers": {
    "container_manager": {
      "command": "python",
      "args": [
        "tools/manage_container.py",
        "--mcp"
      ],
      "env": {
        "DOCKER_CONTEXT": "desktop-linux",
        "DOCKER_CONFIG": "${USERPROFILE}\\.docker"
      }
    },
    "docker_tools": {
      "command": "python",
      "args": [
        "tools/docker_tools.py"
      ],
      "env": {
        "DOCKER_CONTEXT": "desktop-linux",
        "DOCKER_CONFIG": "${USERPROFILE}\\.docker",
        "DOCKER_TOOLS_DEBUG": "1"
      },
      "tools_timeout": 240
    },
    "sequential_thinking": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-sequential-thinking"
      ],
      "connect_timeout": 60,
      "init_timeout": 60,
      "tools_timeout": 30
    },
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest"
      ],
      "connect_timeout": 60,
      "init_timeout": 60,
      "tools_timeout": 30
    }
  }
}
